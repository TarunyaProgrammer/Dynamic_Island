# lefthook.yml — Git hooks configuration
# https://github.com/evilmartians/lefthook

pre-commit:
  parallel: false
  commands:
    test:
      run: npm test
      fail_text: "Unit tests failed. Fix errors before committing."
